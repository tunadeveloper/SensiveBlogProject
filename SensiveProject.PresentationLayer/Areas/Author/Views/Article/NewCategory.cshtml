@* @model ArticleCategoryViewModel
<form method="post" asp-action="NewCategory" asp-controller="Article">
    <div class="tab-content" id="nav-tabContent">
      @*   <div class="tab-pane fade @(ViewData["ActiveTab"]?.ToString() == "category" ? "show active" : "")" id="new_category" role="tabpanel" aria-labelledby="nav-home"> *@
   @*          <div class="msg_list_main">
                <label>Kategori Adı</label>
                <input type="text" asp-for="Category.CategoryName" class="form-control" />
                <span class="text-danger" asp-validation-for="Category.CategoryName"></span>
                <br />
                <button type="submit" class="btn cur-p btn-success">Kategoriyi Ekle</button>
            </div>
        </div>
    </div>
</form> *@ 